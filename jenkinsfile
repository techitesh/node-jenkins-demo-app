pipeline {
    agent any
    tools {nodejs "nodejs"}

    stages {
        stage('Build') {
            steps {
                sh 'npm install'
            }
        }

        stage('deploy') {
            steps {
                echo 'Deploying APPlication'
            }
        }
    }
}