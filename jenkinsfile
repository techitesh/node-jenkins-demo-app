pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building Artifacts'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing Artifacts'
            }
        }

        stage('deploy') {
            steps {
                echo 'Deploying APPlication'
            }
        }
    }
}